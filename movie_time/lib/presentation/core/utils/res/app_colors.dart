import 'package:flutter/material.dart';

class AppColors {
  // Main app colors
  static const systemNavbar = Colors.white;
  static const Brightness navbarIconBrightness = Brightness.dark;
  static const primary = Colors.blue;
  static const accent = Colors.blueAccent;

  // Custom Back button
  static const backButtonBackground = Colors.white54;

  // Shared colors among multiple pages
  static const subtitle = Colors.grey;

  // Actor page
  static const actorText  = const Color(0xff757575);
  static const actorBirthdayPrefix = const Color(0xff424242);

  // Custom list item
  static const defaultCustomListItemBackground = Colors.white;
  static const defaultCustomListItemText = Colors.black;

  // Try again page
  static const tryAgainButtonBackground = Colors.blue;

  // No-image widget
  static const noImageBackground = Colors.grey;

  // Star rating
  static const star = Colors.yellow;

  // Home page
  static const appBarBackground = Colors.white54;
  static const appBarIconBackground = Colors.transparent;
  static const categoryBackgroundUnselected = const Color(0x669e9e9e);
  static const categoryBackgroundSelected = Colors.blue;
  static const categoryTextSelected = const Color(0xd2000000);
  static const categoryTextUnselected = Colors.white;
  static const homeMovieTitle = const Color(0xccffffff);
  static const homeMovieTitleShadow = Colors.black;
  // Home page - web
  static const trailerPlayButtonWeb = Colors.red;
  static const genresAndRatingWeb = const Color(0xff616161);

  // Movie page
  static const defaultMovieBackground = Colors.white;
  static const defaultMovieText = Colors.black;
  static const defaultMovieSubtitle = Colors.black;

  // Search page
  static const searchBarFill = Colors.white;
  static const searchedMovieBackground = Colors.white;
  static const searchedMovieRating = const Color(0xff757575);

  // 
}